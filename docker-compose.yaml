version: '3.8'

services:
  jupyter-env:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - .:/workspace
    environment:
      - JUPYTER_TOKEN=cs4210
    command: jupyter lab --ip=0.0.0.0 --port=8888 --no-browser --allow-root